library("graphics") # For spider graphs

#install.packages('plotrix')
library(plotrix)


if(!require(installr)) {
	install.packages("installr"); 
	require(installr)
} #load / install+load installr
updateR()
